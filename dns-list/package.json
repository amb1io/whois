{
  "name": "dns-list",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled --persist-to ../.wrangler",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "migrate:local": "wrangler d1 migrations apply demos --local --persist-to ../.wrangler",
    "migrate:remote": "wrangler d1 migrations apply demos --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250613.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.19.0"
  }
}
